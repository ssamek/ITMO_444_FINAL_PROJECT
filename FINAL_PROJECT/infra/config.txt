#!/usr/bin/env bash


# AWS region
REGION="us-east-1"

# Unique S3 bucket name (must be globally unique)
RESUME_BUCKET_NAME=

# VPC/Subnet CIDRs
VPC_CIDR="10.10.0.0/16"
SUBNET_CIDR_1="10.10.1.0/24"
SUBNET_CIDR_2="10.10.2.0/24"

# EC2/Key settings - create keypair in AWS console or let script create (see CREATE_KEYPAIR)
#KEY_NAME=my-key-pair
KEY_NAME=my-key-pair-$(date +%s)
KEY_FILE=my-key-pair.pem
KEY_TYPE=ed25519 

# EC2 instance
INSTANCE_TYPE="t3.micro"
AMI_ID=ami-03deb8c961063af8c
UBUNTU_OWNER=099720109477
UBUNTU_FILTER=ami-03deb8c961063af8c
#UBUNTU_FILTER=UBUNTU_FILTER=ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*

# Security group settings
SECURITY_GROUP_NAME=ResumeAppSG
SECURITY_GROUP_DESC="SSH & HTTP access"

# Tag used for all resources created by these scripts (helps teardown)
PROJECT_TAG_KEY="Project"
PROJECT_TAG_VALUE="resume-uploader"

# CloudWatch log group/stream (optional)
CW_LOG_GROUP=resume-parser-logs
CW_LOG_STREAM=automation-run
CW_METRIC_NAMESPACE=ResumeAutomation
CW_METRIC_NAME=StepsCompleted

# Auto teardown hours (used by schedule_teardown.sh)
AUTO_TEARDOWN_HOURS=24

# Whether the scripts should attempt to create a new key pair if KEY_FILE doesn't exist.
CREATE_KEYPAIR=true
